server:
  port: 8082

spring:
  application:
    name: validator-service

logging:
  level:
    org.org.springframework.web: DEBUG
    com.akirolabs.validator:
      - DEBUG
      - INFO

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

